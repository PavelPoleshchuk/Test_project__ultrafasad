function NotFoundPage() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h2>NotFoundPage</h2>
    </div>
  );
}

export default NotFoundPage;
